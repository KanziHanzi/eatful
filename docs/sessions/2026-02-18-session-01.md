# Session Gist - 2026-02-18

## What Was Built

- Removed starter tab/template structure and moved to a single-screen app.
- Implemented food diary capture flow using device camera.
- Rendered captured meal/drink photos in a 3-column grid.
- Added per-photo timestamps in 24h format.
- Replaced standalone capture button with an in-grid add tile at the end.

## Persistence and Data Model

- Added local persistence with AsyncStorage.
- Grouped entries by date into day objects:
  - `DiaryDay { dateKey, timestamp, entries }`
  - `DiaryEntry { id, uri, takenAt }`
- Added top-left `Clear` action to clear local cache.

## UI Adjustments Applied

- Header simplified to centered `Today` only.
- Add tile styled as transparent with dark/light border and plus icon.
- Removed add-tile subtext.
- Removed tile shadow/elevation.
- Ensured safe area handling and status bar/background alignment.

## Project Structure Refactor

- Moved app logic into `src/*` structure.
- Added component folders with separate style files.
- Added centralized exports via `src/components/index.ts`.
- Extracted reusable utilities into `src/utils`.
- Moved assets to `src/assets` and updated `app.json` paths.
- Updated `tsconfig.json` for `src/*` absolute imports.

## Governance / Conventions

- Created `conventions.md` and expanded it with team rules:
  - composition over inheritance
  - context over prop drilling
  - arrow functions
  - naming/file conventions
  - separate style files
  - component folder strategy
  - utils/hooks camelCase
  - assets in `src/assets`

## Git Outcome

- Created commit:
  - `ba7b602`
  - `refactor app to single-screen diary foundation and src conventions`
- Remaining unstaged local change at commit time: `.vscode/settings.json`.

## Follow-ups for Next Session

- Optionally persist images more robustly (URI validity across restarts/platforms).
- Optionally render previous days as date-grouped sections below Today.
- Optionally enforce conventions with lint/import rules.
