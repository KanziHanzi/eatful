import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  card: {
    flex: 1,
    maxWidth: '32%',
    gap: 6,
  },
  image: {
    width: '100%',
    aspectRatio: 1,
    borderRadius: 10,
    backgroundColor: 'transparent',
  },
});
